# core

```
make prepare #download dependencies, by default master branches are downloaded
make parsers=<branch or tag> graphchi-version=<branch or tag> modeling-version=<branch or tag> prepare # prepare a specific branch/tag of dependencies
make graphchi-version=selective prepare # download parsers, grapchi and modeling dependencies. graphchi use selective branch/tag
```
